# Disk cleanup ledger — 2026-07-14

This cleanup was authorized to make room for the remaining Tier A experiments.
Only reproducible intermediates and caches are removed. Source code, frozen
configs, source snapshots, query banks, checkpoints, seed-level tables,
summaries, validation files, figures, and the final report are preserved.

## Removed project intermediates

- `runs/e1_confirmation_20260714/scores_seed_average/` (~110 MB):
  per-observation arrays already reduced into seed-level and summary artifacts.
- `runs/e4_confirmation_20260714/scores_seed_average/` (~443 MB):
  per-observation arrays already reduced into detection, seed-level, and summary
  artifacts.
- `runs/development_milestone_20260714/scores/` (~66 MB): obsolete development
  score dump; the corrected `v2` summary is the report source.
- `runs/development_milestone_20260714_v2/scores/` (~66 MB): development-only
  per-observation arrays already reduced into the E2 perturbation and gate tables.
- `runs/e4_development_20260714/scores/` (~30 MB): development-only score arrays
  superseded by the frozen frequency-bank amendment and confirmation summaries.

## Removed user caches

- `~/Library/Caches/ms-playwright/` (~539 MB): rebuildable browser runtime cache.
- `~/Library/Caches/node-gyp/` (~62 MB): rebuildable native-build cache.
- `~/Library/Caches/Homebrew/` (~43 MB): rebuildable package-manager cache.
- `~/Library/Caches/pip/` (~1.5 MB): rebuildable Python package cache.

No raw user datasets or irreplaceable experiment results were removed.

## Second pass after macOS swap pressure

The first neural-flow smoke caused macOS swap to expand by roughly 2.5 GB.
To preserve the 1 GB hard disk guard, the following rebuildable components of
the bundled Codex workspace runtime cache were removed. The Poppler runtime was
retained for PDF rendering.

- `~/.cache/codex-runtimes/.../dependencies/node/` (~401 MB)
- `~/.cache/codex-runtimes/.../dependencies/python/` (~404 MB)
- `~/.cache/codex-runtimes/.../dependencies/native/libreoffice-headless/`
  (~410 MB)
- `~/.cache/codex-runtimes/.../dependencies/native/git/` (~141 MB)

These are cached tool runtimes, not project environments or experimental data;
Codex may re-download them if a later document task needs them.

## Final state after the remaining runs

- The selected experiment-run tree is approximately 199 MB.
- The completed report package, including rendered QA pages, is approximately
  4.9 MB.
- Approximately 2.0 GiB remained free after all confirmation runs, analysis,
  LaTeX compilation, and PDF rendering. Free space temporarily rose to 3.9 GiB
  after swap receded.
- No additional result artifacts were deleted after the confirmation runs.

## Adjudication-stage cleanup

- `~/.cache/uv/` (~853 MB): package downloads created by an accidental
  project-local environment bootstrap; the existing project environment was
  retained and used instead.
- `~/Library/Caches/com.anthropic.claudefordesktop.ShipIt/` (~741 MB):
  rebuildable application-update cache removed after the 1 GiB experiment
  disk guard stopped three development jobs before they produced scientific
  rows.
- The newly created temporary `distributional_sid/.venv/` was removed; the
  pre-existing workspace `.venv/` remains intact.

No experiment outputs or source files were removed in this pass.

## Final rebuildable-cache pass before adjudication

To keep the hard experiment guard above 1 GiB while macOS retained several
gigabytes of swap, these additional rebuildable caches were removed:

- `~/.npm/_cacache/` and `~/.npm/_npx/`
- `~/Library/Caches/Google/`
- `~/Library/Caches/com.microsoft.VSCode.ShipIt/`
- `~/Library/Caches/GeoServices/`
- `~/.cache/fontconfig/`
- `~/Library/Caches/Codex/`
- Claude `Cache/` and `Code Cache/`
- Chrome component, optimization-guide, and Safe Browsing caches

Application data, browser profiles, credentials, source code, experiment
outputs, frozen configurations, and model/result artifacts were preserved.

## State after adjudication and final report QA

- The adjudication report package is approximately 5.2 MB, including the PDF,
  source, figures, tables, and rendered QA pages.
- Approximately 2.3 GiB was free after all new experiments, the 30-seed E9
  replication, plot generation, LaTeX compilation, and the final test suite.
- The final package test suite passed 20/20.
